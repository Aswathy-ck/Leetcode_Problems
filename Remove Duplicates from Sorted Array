class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
       
        
        a = [nums[0]] 
        for i in range(1,len(nums)):
            if(nums[i]!=nums[i-1]):
                a.append(nums[i])
        nums[:]=a
         
        print(a)
        
	   
